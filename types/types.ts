export interface Products {
  id: number;
  created_at: string;
  name: string;

  type: string;
}

export interface ShoppingList {
  id: number;
  created_at: string;
  name: string;

  type: string;
}
